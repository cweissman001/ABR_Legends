using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EncodedObject : MonoBehaviour
{
    public ABREncoding Encoding { get; set; }
    public ABRDataScene dataScene;
    public EncodingRenderInfo encodingRenderInfo;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
